# AGENTS.md ? ??????

## ????
- ?? **Vue 3 + Express + SQLite** ?????????????????
- ???????????????
- ????/??????
- ?????????????ICP/???????????Emoji?

## ???
| ? | ?? |
|----|------|
| ?? | Vue 3 + Vue Router + Vite |
| ?? | Tailwind CSS v4 (`@custom-variant dark`) |
| ?? | Express 5 (ES Module) |
| ??? | sql.js?? JS SQLite?????? |
| ?? | SHA-256 ?? |
| ?? | Node.js ?18 |

## ????
```
src/views/HomePage.vue    ????
src/views/AdminPage.vue   ????
src/router/index.js       ??
src/style.css             Tailwind + ??
server.js                 Express?????????
db/database.js            SQLite ????
```

## ??
```bash
npm install        # ????
npm run build      # ????
npm run dev        # ?????/?????
```

## ???nginx + Linux?
?? `../Personal_index_extra/deploy/server-prod.js`?? API ??
```bash
# nginx ?????? + ???? /api/ ? 127.0.0.1:3456
pm2 start server-prod.js --name personal-api
```

## Git ???
```bash
git pull
npm install
npm run build
pm2 restart personal-api
```

## ????
- ???? UTF-8 ? BOM?PowerShell `Out-File -Encoding UTF8` ? BOM ????
- Vue ?? `<script setup>` ??
- ?/??????????? `dark:` ??
- sql.js ? `exec()` ???????? `prepare()` + `bind()` + `step()`
